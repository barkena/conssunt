   const config = {
       source: 'data.json', // Example: Data source is a JSON file named 'data.json'
       p: {
           // Specific properties related to the source
           format: 'json',
           delimiter: ','
       }
   };
   