define('Lab', ['Color'], function(Color) {
    var Lab = extend(Color, {
        // Module Lab extends Color
        // Define properties and methods specific to Lab here
    });

    return Lab;
});
